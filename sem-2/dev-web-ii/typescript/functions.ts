function imprimir(){
    console.log("ola")
}

imprimir()

const soma = (a: number, b: number): number => a + b; 
console.log(soma(1, 2));

function sub(a: number, b: number): number {
    return a - b;
}

console.log(sub(5,4));